{
  "name": "hidden-columbus",
  "version": "1.0.0",
  "description": "Columbus's Best Kept Secrets - Event discovery platform",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site - no build needed'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  }
}
